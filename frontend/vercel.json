{
    "version":2,
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/static-build",
            "confing": {"distDir": "dist"}
        }
    ],
    "rewrites": [
      {
        "source": "/(.*)",
        "destination": "/"
      }
    ]
}